name: mission_control_mobile
description: Mission Control for mobile - manage agents, approvals, tasks on the go
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  sqflite: ^2.3.0
  path: ^1.9.0
  http: ^1.2.0
  intl: ^0.19.0
  shared_preferences: ^2.2.0
  flutter_local_notifications: ^17.2.0
  permission_handler: ^11.3.0
  url_launcher: ^6.2.0
  connectivity_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
